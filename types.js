/* @flow */

/* ::
export type ParsedKey = {
  users: ParsedUser[],
};
export type ParsedUser = {
  revocationCertifications?: Object[],
  userId?: ParsedUserId,
};
export type ParsedUserId = {
  userid?: string,
};
*/
